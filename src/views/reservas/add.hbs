<div class="container p-4">
    <div class="row">
        <div class="col-md-6 col-lg-4 col-xl-4 mx-auto">
            <div class="card" id="card-reservas">
                <div class="card-body">
                    <form action="/reservas/add" method="POST">
                        <img class="img-logo mx-auto d-block" src="/images/logo.jpg" alt="logo">
                        <h2 class="card-title text-uppercase">Reservar</h2>
                        <h5>Tratamientos:</h5>
                        {{!-- <a href="">Tratamientos: </a> --}}
                        <select name="id_tratamiento">
                            <option value="{{id_tratamiento}}" selected="selected">Seleccionar: </option>
                            {{#each tratamientos}}
                            <option value='{{id_tratamiento}}'>{{nombre_tratamiento}} {{duracion_tratamiento}} minutos
                            </option>
                            {{/each}}
                        </select>
                        <h5>Fecha a reservar:</h5>
                        {{!-- <a href="">Fecha a reservar</a> --}}
                        <div class="form-group">
                            <input type="date" min={{today}} name="fecha_reservada" class="form-control"
                                placeholder="Fecha">
                        </div>
                        <h5>Hora deseada:</h5>
                        {{!-- <a href="">Hora deseada</a> --}}
                        <div class="form-group">
                            <input type="time" name="hora_reservada" min="09:00" max="18:00" step="1800"
                                class="form-control">
                        </div>
                        <div class="form-group">
                            <button class="btn btn-success btn-block">
                                Guardar
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>